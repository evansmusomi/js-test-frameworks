'use strict';

const Math = {
    add(first, second){
        return first + second;
    },
    subtract(first, second){
        return first - second;
    },
    multiply(first, second){
        return first * second;
    },
    divide(first, second){
        return first / second;
    }
}

module.exports = Math;